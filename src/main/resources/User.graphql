schema {
    query: Query
    mutation: Mutation
}

type Mutation {
    addUser(email: String!, username: String!, password: String!): String
}

type Query{
    users: [User]
    user(id: String): String
}

type User{
    userId: Int
    email: String
    username: String
    password: String
}